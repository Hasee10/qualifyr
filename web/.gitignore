# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*
!.env.local.example
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# Copied in from the repo root by vercel.json's installCommand so the Python function
# can import them (Root Directory is web/, so nothing above it exists at runtime).
# Build artefacts, not source - the originals live at the repo root.
/gtm_engine/
/config/
/api/__pycache__/

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.idea/
.vscode/
*.swp
*.swo

# Screenshot (uncomment if you want to exclude large screenshots)
# screenshot.png